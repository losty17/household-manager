DATABASE_URL=postgresql://postgres:postgres@db:5432/household_manager
SECRET_KEY=change-me-to-a-long-random-secret
APP_PASSWORD=change-me-to-your-access-password
# Web Push (VAPID) keys – generate with: python -c "from py_vapid import Vapid; v=Vapid(); v.generate_keys(); print('PRIVATE:', v.private_pem().decode()); print('PUBLIC:', v.public_key.public_bytes(__import__('cryptography').hazmat.primitives.serialization.Encoding.X962, __import__('cryptography').hazmat.primitives.serialization.PublicFormat.UncompressedPoint).hex())"
VAPID_PRIVATE_KEY=
VAPID_PUBLIC_KEY=
VAPID_CLAIMS_SUB=mailto:admin@household-manager.local
